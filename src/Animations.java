/**
 * An enum method constructor for the all the animation method.
 * Move: the movement of the shape for the animation.
 * SCALE: the dimension of the shape – sizes.
 * COLORCHANGE: the colour that being change from the original implement colour to
 *     the new colour that the shape need to change to.
 */
public enum Animations {
  MOVING, SCALING, COLORCHANGE;
}
